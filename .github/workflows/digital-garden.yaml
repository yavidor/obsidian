name: Update the Digital Garden Repository

on:
  push:
    branches:
      - main

concurrency:
  group: "updates"
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  call-garden-workflow:
    uses: yavidor/digital-garden/.github/workflows/update-contents.yaml@v4

